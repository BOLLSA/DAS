# Dark Age Saga · 发布前检查清单（RELEASE CHECKLIST）

> 每次改完代码或准备发布正式版本时，逐项核对。可人工核对或让 AI 按此清单复查。
> 与「AI_HANDOVER（游戏有改动随时更新我）.md」分开维护；发布流程见交接文档 §17-15。

---

## 一、调试痕迹清理

- [ ] 所有 `showToast` 调用是否都必要？是否有仅用于调试的提示？（排查含 `side=`、`测试`、`调试`、`debug` 字样）
- [ ] 所有 `console.log` 是否已删除或改为条件输出？（`addLog` 内的 `console.log(msg)` 为设计行为：战斗日志同步到控制台，保留）
- [ ] 所有 `debugger` 语句是否已移除？（全库应为 0）
- [ ] `console.error` 是否全部用于异常捕获？（正常应为 21 处左右，均为 try/catch 内错误记录，保留）

## 二、关键词残留扫描

- [ ] 是否包含 `side=0`、`side=1`、`(side=$` 等调试字符串？
- [ ] 是否包含 `临时`、`调试`、`测试用`、`debug`、`XXX` 等注释残留？（`调试工具` 为测试面板正常 UI 文案，例外）
- [ ] 全局搜索 `TODO`、`FIXME`、`HACK`，确认是否都已处理？（应为 0）

## 三、联机快照安全

- [ ] `networkSerializeState` 是否剔除了本地调试/UI 字段？
  - [ ] `matchEvents` → 剔除为 `[]`（复盘事件仅主机需要）
  - [ ] `isModalOpen` → 恒置 `false`（弹窗互斥锁是本地 UI 状态）
  - [ ] 无 `_tmp`、`__debug`、`_debug*` 等临时字段泄漏
  - [ ] 手牌/牌堆/预牌堆卡对象精简为 `{n,d,db,dt}`（避免发送完整卡对象）
- [ ] 快照恢复（`networkRestoreCard`）是否与精简字段一一对应？

## 四、测试面板限制

- [ ] `openTestPanel` 在以下模式是否被禁用？
  - [ ] 自定义卡组模式（`gameMode === 'custom'`）
  - [ ] 人机对战模式（`gameMode === 'ai'`）
  - [ ] 新手教程中（`tutorialState.active`）
  - [ ] 远程联机中（`networkActive()`）
- [ ] 是否确认全卡池模式（正式对战）下测试面板的可用性策略？（当前可用，如需正式版隐藏需调整）

## 五、完整回归（发布前必做）

- [ ] 15 个 JS 文件 `node --check` 语法检查全部通过（运行 `check.ps1`）
- [ ] 全卡池双人对战完整一局（放置/移动/攻击/技能/结束回合/攻击城池）
- [ ] 人机对战（简单 + 大师难度各一局）
- [ ] 测试模式快速验证改动
- [ ] 新手教程完整走一遍
- [ ] 远程联机（主机 + 客机各一局，含满手牌结束回合、防御弹窗）
- [ ] 图鉴/卡池查看：等级筛选、搜索、我方/敌方切换（预设卡组模式）
- [ ] 悬赏机制：3/5/7/9 连杀升级、被移除发放赏金、复活重置

## 六、文档同步

- [ ] `AI_HANDOVER（游戏有改动随时更新我）.md` 中的统计数字（代码行数/卡牌数/装备数/技能数）已更新（标注"约"字）
- [ ] `Dark Age Saga更新日志.md` 顶部已记录本次改动
- [ ] 版本号（`index.html` 3 处 + `decks.js` 标题）已同步

## 七、发布

- [ ] 运行 `publish.ps1 -FolderName "Dark Age Saga<本次改动一句话总结>"` 发布到 `C:\Users\WhereIt\Desktop\DAS\正式版本\1.01\`
- [ ] 源文件数与目标文件数一致（脚本会自动核对）
- [ ] DAS 根目录交接文档已同步

---

## 最近一次核对记录

| 日期 | 核对人/方式 | 结果 | 备注 |
|------|------------|------|------|
| （待填写） | AI 复查 | 待核 | debugger=0 / console.log=1(设计) / console.error=21(异常) / 调试toast=0 |
